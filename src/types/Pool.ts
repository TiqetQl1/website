export type Configs = {
    organizer : string,
    time_end : bigint,
    time_start : bigint,
    ticket_price_wei: bigint,
    max_tickets_total : bigint,
    max_tickets_per_participant : bigint,
    winners_count : bigint,
    percent_per_nft : bigint,
    percent_per_winner : bigint,
}
